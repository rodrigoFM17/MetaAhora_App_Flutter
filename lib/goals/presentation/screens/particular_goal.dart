import 'package:flutter/widgets.dart';

class ParticularGoal extends StatelessWidget {

  final String goalId;
  const ParticularGoal({super.key, required this.goalId});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}